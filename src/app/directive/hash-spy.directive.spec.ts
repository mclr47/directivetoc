import { HashSpyDirective } from './hash-spy.directive';

describe('HashSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new HashSpyDirective();
    expect(directive).toBeTruthy();
  });
});
